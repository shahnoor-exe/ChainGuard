fastapi==0.111.0
uvicorn[standard]==0.29.0
httpx==0.27.0
python-dotenv==1.0.1
requests==2.31.0

# Lightweight ML — replaces heavy XGBoost + full sklearn pipelines
scikit-learn==1.4.2
numpy==1.26.4
pandas==2.2.2

# Remove spaCy entirely — use regex instead (saves ~250MB RAM)
# spacy                  ← DELETED
# en_core_web_sm         ← DELETED

# Keep these
python-dateutil==2.9.0
